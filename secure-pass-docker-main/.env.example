# ==============================================
# SECUREPASS - MULTI-TENANT CONFIGURATION
# ==============================================
# Este archivo contiene todas las variables de entorno necesarias
# para ejecutar SecurePass en modo multi-empresa con Docker
#
# Instrucciones:
# 1. Copiar este archivo como .env: cp .env.example .env
# 2. Configurar las variables según tu entorno
# 3. NUNCA commitear el archivo .env al repositorio
# ==============================================

# ----------------------------------------------
# CONFIGURACIÓN GENERAL
# ----------------------------------------------
NODE_ENV=development
# Opciones: development, production
# Para producción usar: NODE_ENV=production BUILD_TARGET=production

BUILD_TARGET=development
# Opciones: development, production
# Determina qué stage del Dockerfile usar

# ----------------------------------------------
# MONGODB - BASE DE DATOS
# ----------------------------------------------
MONGO_ROOT_USER=admin
# Usuario root de MongoDB

MONGO_ROOT_PASSWORD=securepass2024
# Contraseña root de MongoDB (¡CAMBIAR EN PRODUCCIÓN!)

MONGO_DB_NAME=securepass
# Nombre de la base de datos

MONGODB_PORT=27017
# Puerto de MongoDB (default: 27017)

MONGODB_URI=mongodb://admin:securepass2024@mongodb:27017/securepass?authSource=admin
# URI completa de conexión a MongoDB (se construye automáticamente)

# ----------------------------------------------
# API BACKEND
# ----------------------------------------------
API_PORT=8000
# Puerto donde correrá la API

JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Secret para firmar tokens JWT (¡CAMBIAR EN PRODUCCIÓN!)
# Debe ser una cadena aleatoria y segura de al menos 32 caracteres

# ----------------------------------------------
# FRONTEND WEB
# ----------------------------------------------
WEB_PORT=3000
# Puerto donde correrá el frontend

REACT_APP_API_URL=http://localhost:8000/api
# URL de la API que usará el frontend
# En producción usar la URL real de tu dominio

# ----------------------------------------------
# NGINX REVERSE PROXY
# ----------------------------------------------
NGINX_HTTP_PORT=80
# Puerto HTTP de Nginx

NGINX_HTTPS_PORT=443
# Puerto HTTPS de Nginx

# ----------------------------------------------
# CLOUDINARY - ALMACENAMIENTO DE IMÁGENES
# ----------------------------------------------
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
# Nombre de tu cloud en Cloudinary
# Obtener en: https://cloudinary.com/console

CLOUDINARY_API_KEY=your-cloudinary-api-key
# API Key de Cloudinary

CLOUDINARY_API_SECRET=your-cloudinary-api-secret
# API Secret de Cloudinary

# ----------------------------------------------
# EMAIL - NOTIFICACIONES
# ----------------------------------------------
EMAIL_HOST=smtp.gmail.com
# Servidor SMTP para envío de emails
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
# SendGrid: smtp.sendgrid.net

EMAIL_PORT=587
# Puerto SMTP (generalmente 587 para TLS)

EMAIL_USER=your-email@gmail.com
# Email desde el cual se enviarán las notificaciones

EMAIL_PASSWORD=your-app-password
# Contraseña del email o App Password
# Para Gmail: https://support.google.com/accounts/answer/185833

# ----------------------------------------------
# MULTI-TENANT - CONFIGURACIÓN DE EMPRESAS
# ----------------------------------------------
# Estas variables se pueden configurar por empresa
# y se gestionan desde el panel de administración

DEFAULT_COMPANY_SUBDOMAIN=demo
# Subdominio por defecto para testing

DEFAULT_COMPANY_NAME=Demo Company
# Nombre de la empresa de demostración

DEFAULT_PLAN=free
# Plan por defecto para nuevas empresas
# Opciones: free, basic, premium, enterprise

MAX_USERS_FREE=10
# Número máximo de usuarios en plan free

MAX_RESIDENTS_FREE=50
# Número máximo de residentes en plan free

# ----------------------------------------------
# SEGURIDAD
# ----------------------------------------------
# Configuración adicional de seguridad

CORS_ORIGIN=http://localhost:3000
# Origen permitido para CORS
# En producción usar tu dominio real

RATE_LIMIT_MAX=100
# Número máximo de requests por ventana de tiempo

RATE_LIMIT_WINDOW=15
# Ventana de tiempo en minutos para rate limiting

# ----------------------------------------------
# LOGS Y MONITOREO
# ----------------------------------------------
LOG_LEVEL=info
# Nivel de logging
# Opciones: error, warn, info, debug

# ----------------------------------------------
# DESARROLLO
# ----------------------------------------------
# Variables solo para desarrollo local

DEV_SEED_DATABASE=false
# Poblar la base de datos con datos de prueba

DEV_SUPER_ADMIN_EMAIL=admin@securepass.com
# Email del super admin para desarrollo

DEV_SUPER_ADMIN_PASSWORD=Admin123!
# Contraseña del super admin para desarrollo

# ==============================================
# NOTAS IMPORTANTES
# ==============================================
#
# 1. SEGURIDAD:
#    - Cambiar todos los secrets en producción
#    - Usar contraseñas fuertes y únicas
#    - No compartir este archivo .env
#
# 2. CLOUDINARY:
#    - Crear cuenta gratuita en cloudinary.com
#    - Configurar folder structure para multi-tenant
#
# 3. EMAIL:
#    - Para Gmail, habilitar "App Passwords"
#    - Verificar límites de envío de tu proveedor
#
# 4. DOCKER:
#    - Para desarrollo: docker-compose up
#    - Para producción: docker-compose -f docker-compose.prod.yml up -d
#
# 5. MULTI-TENANT:
#    - Cada empresa tiene su propio subdominio
#    - Los datos se filtran automáticamente por empresa
#    - El logo se almacena en Cloudinary
#
# ==============================================
